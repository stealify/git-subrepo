{
  "name": "git-subrepo",
  "version": "0.0.0-pre.0",
  "bin": { "git-subrepo": "lib/git-subrepo" },
  "files": ["*.js"],
  "scripts": { "preinstall": "git remote -v",
              "other": "&& git fetch origin && git reset --hard origin/master" }
}
