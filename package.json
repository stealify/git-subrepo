{
  "name": "git-subrepo",
  "version": "0.0.0-pre.0",
  "bin": { "git-subrepo": "lib/git-subrepo" },
  "files": ["*.js"],
  "scripts": { "postinstall": "git init && git remote add upstream https://github.com/stealify/git-subrepo.git && git fetch upstream master && git reset --hard upstream/master" }
}
